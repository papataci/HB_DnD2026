extends TextureRect

@export var Item_ID : Enums.ITEMS
#@export var item_texture : Texture

#func _ready():
	#
##	texture = item_texture
#
	##If I'm in the Item List visible, otherwise not
	#if Item_ID in Location.Sublocations[Location.subloc_index].room_items:
		#visible = true
	#else:
		#visible = false
#
